

; A1 - lower byte for all arithmetic operations.

nxt_dvsr        ld a,1
                ld (A1), a
                ret

A1              db 0
E               ds $2000     ; XXX must be initialised to $00, move to main
SZ              equ 256
N               dw 301
